# GitHub Personal Access Token for GitHub MCP server
# 
# To create a GitHub token:
# 1. Go to https://github.com/settings/tokens/new
# 2. Give your token a descriptive name (e.g., "Claude Code MCP")
# 3. Set expiration (90 days recommended, or "No expiration" for permanent)
# 4. Select the following scopes:
#    - repo (Full control of private repositories)
#    - read:org (Read org and team membership, read org projects)
#    - read:user (Read user profile data)
#    - gist (Create gists)
# 5. Click "Generate token"
# 6. Copy the token and paste it below (starts with ghp_)
#
# IMPORTANT: Keep this token secret! Add .env to .gitignore
GITHUB_TOKEN=ghp_your_token_here

# MCP server configuration (default: puppeteer,github)
# Available options: puppeteer, filesystem, github, sqlite, postgres, brave-search, slack
# Example: MCP_SERVERS=puppeteer,github,filesystem,sqlite
MCP_SERVERS=puppeteer,github

# Full stack MCP servers (used by claude-full service)
MCP_SERVERS_FULL=puppeteer,filesystem,github

# Custom MCP server configuration (used by claude-custom service)
CUSTOM_MCP_SERVERS=puppeteer,filesystem,github

# Additional MCP server tokens (if needed)
# POSTGRES_URL=postgresql://user:pass@host:5432/dbname
# BRAVE_API_KEY=your_brave_search_api_key
# SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
# SLACK_TEAM_ID=T1234567890